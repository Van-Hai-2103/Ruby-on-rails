<div class="custom-form-container">
  <p class="back-link"><%= link_to "Back", products_path %></p>
  <section class="product">
    <div class="product-image">
      <%= image_tag @product.featured_image if @product.featured_image.attached? %>
    </div>
    <section class="product-info">
      <% cache @product do %>
        <h1 class="product-title"><%= @product.name %></h1>
        <p class="product-description"><%= @product.description %></p>
      <% end %>
      <%= render "inventory", product: @product %>
      # <% if authenticated? %>
      #   <div class="product-actions">
      #     <%= link_to "Edit", edit_product_path(@product), class: "btn edit-btn" %>
      #     <%= button_to "Delete", @product, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "btn delete-btn" %>
      #   </div>
      # <% end %>
    </section>
  </section>
  <%= link_to "Cancel", products_path, class: "cancel-link" %>
</div>


